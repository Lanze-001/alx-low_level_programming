#include "lists.h"

/**
 * reverse_listint - defines the function.
 * @head: describes the argument.
 * Return: A pointer to the first node of the reversed list.
 */
listint_t *reverse_listint(listint_t **head)
{
	head++;
	return (0);
}
